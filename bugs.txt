1. коли поля в ордері заповнені до того, як загрузилась сторінка, автоматично не прокладається шлях		+
2. не створюється водій																					+
3. не можу відправити замовленняю хз чого																+
4. на карті голубий кружечок лишній																		не факт, один з них може й лишній, тільки треба подумати який
5. перший раз часто на хрещатик кидає GPS																тут вже не я винуватий
6. на index.html if user.is_authentificate === true, а на інших сторінках false, хоч я залогінений		покажеш, поки не поняв, де баг
7. /rating/ помилка																						так і не поняв, де там помилка?